# Reparto interno — Tarea 3

| Integrante | Aporte |
|---|---|
| **Isaac** | Diseño general del TAD y de la búsqueda binaria (`_buscar`/`_localizar`); implementación de los métodos de **consulta** en `agenda.py` (`__init__`, `__len__`, `contiene`, `telefono_de`, `nombres`) con sus docstrings y complejidad; `medicion.py` completo y la interpretación de los resultados en `resultados.md` |
| **Santiago** | Implementación de los métodos de **modificación** en `agenda.py` (`agregar`, `eliminar`), con sus docstrings, complejidad y el manejo de `ValueError`/`KeyError`; revisión de que las 14 pruebas cubran esos dos métodos y aporte de 1-2 pruebas nuevas si encuentra un caso borde que falte |
| **Leimar** | Correr las pruebas y `medicion.py` en su propia máquina; confirmar que los resultados son consistentes (mismo orden de magnitud) y dejar constancia en `resultados.md` |
| **Joseph** | Crear el repositorio en GitHub, subir la estructura inicial del proyecto, escribir/ajustar `README.md` con instrucciones claras de instalación y ejecución |
| **Tobías** | Completar y revisar este `REPARTO.md`; probar `interactivo.py` a mano contra los 7 métodos del TAD y confirmar que no tiene errores |